server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth
    username: postgres
    password: ${AUTH_DB_PASSWORD}
    jpa:
      hibernate:
        ddl-auto: none
      open-in-view: false
      jackson:
        serialization:
          fail-on-empty-beans: false